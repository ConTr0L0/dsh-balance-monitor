/**
 * Host half of dsh-balance-monitor. Plugin entry: real-time balance display,
 * session/daily cost tracking, peak/off-peak pricing and spending limits.
 */
export declare function apply(ctx: any, config?: Record<string, never>): void;
export { PrefsSchema };
